fastapi==0.115.5
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
passlib[bcrypt]==1.7.4
# BUG FIX: bcrypt>=4.0 removed the `__about__.__version__` attribute that
# passlib 1.7.4 probes on every hash/verify call to detect the bcrypt
# version — harmless (passlib catches the AttributeError and continues),
# but it spams "(trapped) error reading bcrypt version" into the logs on
# every single login. Pinning to the last 3.x release passlib expects
# silences it with no behavior change.
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
python-multipart==0.0.12
pydantic[email]==2.9.2
email-validator==2.2.0
aiofiles==24.1.0
psycopg2-binary==2.9.10
openpyxl==3.1.2
yagmail==0.15.293
pywebpush>=1.9.4,<3.0.0
pytz==2024.1
