# Test-only dependencies. NOT installed in production (Render installs
# requirements.txt only) — install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt --break-system-packages
-r requirements.txt
pytest==8.3.3
httpx==0.27.2
alembic==1.13.3
